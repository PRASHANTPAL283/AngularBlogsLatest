import { DatePipeValuesConverterPipe } from './date-pipe-values-converter.pipe';

describe('DatePipeValuesConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePipeValuesConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
