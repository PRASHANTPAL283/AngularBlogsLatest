import { DescriptionLimiterPipe } from './description-limiter.pipe';

describe('DescriptionLimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionLimiterPipe();
    expect(pipe).toBeTruthy();
  });
});
