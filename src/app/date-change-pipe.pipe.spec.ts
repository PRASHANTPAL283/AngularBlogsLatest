import { DateChangePipePipe } from './date-change-pipe.pipe';

describe('DateChangePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateChangePipePipe();
    expect(pipe).toBeTruthy();
  });
});
